<h1>Admin Dashboard</h1>

<div class ="order_list">
  <%@orders.each do |order| %>
    <ul class="orders">
      <%=link_to "Order #{order.id}", order_path(order)%>
      <li> Contents: <br>
      <ol>
          <%order.contents.each do |id, quantity|%>
         <li><strong><%=@beans.find(id.to_i).title%></strong><br>
          Quantity: <%=quantity%> <br>
          <% end %>
          </ol>
          </li>
      <li> Total Price: <%=order.total_price%></li>
      <li> Status: <%=order.status%></li>
      <%= link_to("Cancel", admin_order_path(order, :status => 'Cancelled'), method: :put)%>
      <%= link_to("Paid", admin_order_path(order, :status => 'Paid'), method: :put)%>
      <%= link_to("Completed", admin_order_path(order, :status => 'Completed'), method: :put)%>
      

    </ul>
  <% end %>
</div>
<div class="order-status">
  <p id="ordered-count">Ordered Count: <%= @orders.where(status:"Ordered").count %></p><br>
  <p id="paid-count">Paid Count: <%= @orders.where(status:"Paid").count %></p><br>
  <p id="cancelled-count">Cancelled Count: <%= @orders.where(status:"Cancelled").count %></p><br>
  <p id="completed-count">Completed Count: <%= @orders.where(status:"Completed").count %></p>
</div>


<div>
<h1> </h1>

</div>
